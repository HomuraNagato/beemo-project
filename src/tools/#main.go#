package main

import (
	"context"
	"fmt"
	"net"
	"os"
	"time"

	pb "eve-beemo/proto/gen/proto"
	"google.golang.org/grpc"
)

type toolsServer struct {
	pb.UnimplementedToolsServer
}

func (s *toolsServer) Execute(ctx context.Context, req *pb.ToolRequest) (*pb.ToolResult, error) {
	action := req.GetAction()
	switch action {
	case "get_time":
		now := time.Now().Format(time.RFC3339)
		return &pb.ToolResult{
			Action: action,
			Result: now,
		}, nil
	default:
		// Echo fallback for quick testing.
		result := fmt.Sprintf("action=%s value=%s", action, req.GetValue())
		return &pb.ToolResult{
			Action: action,
			Result: result,
		}, nil
	}
}

func main() {
root@eve-orchestrator:/workspace/src/orchestrator# go run main.go 
eve-orchestrator: starting
svc=llm status=error err=context deadline exceeded
svc=tools status=connected addr=eve-tools:5015
svc=vision status=error err=context deadline exceeded
svc=ui status=error err=context deadline exceeded
svc=tts status=error err=context deadline exceeded
svc=asr status=error err=context deadline exceeded
svc=wakeword status=error err=context deadline exceeded
tools status=ok action=get_time result=2026-03-07T15:29:02Z	addr := os.Getenv("TOOLS_LISTEN_ADDR")
	if addr == "" {
		addr = ":5015"
	}

	lis, err := net.Listen("tcp", addr)
	if err != nil {
		fmt.Printf("eve-tools: listen error addr=%s err=%v\n", addr, err)
		os.Exit(1)
	}

	grpcServer := grpc.NewServer()
	pb.RegisterToolsServer(grpcServer, &toolsServer{})

	fmt.Printf("eve-tools: listening addr=%s\n", addr)
	if err := grpcServer.Serve(lis); err != nil {
		fmt.Printf("eve-tools: serve error err=%v\n", err)
		os.Exit(1)
	}
}
